{% extends 'baseUser.html.twig'%}
{% block body %}
    <div style="display:flex;align-items:center;justify-content:center;margin-top: 70px;padding:10px">

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Add Coins Hystory </h5>

                <!-- General Form Elements -->
                {{ form_start(f) }}
                <label for="inputText" class="col-sm-4 col-form-label">montant Achat </label>
                <div class="col-sm-10">
                    {{form_widget(f.montantAchat,{'attr':{'class':'form-control'}})}}
                </div>
                <label for="inputText" class="col-sm-6 col-form-label">transaction </label>
                <div class="col-sm-10">
                    {{form_widget(f.transaction,{'attr':{'class':'form-control'}})}}
                </div>
                <label for="inputText" class="col-sm-6 col-form-label">Money Recieved </label>
                <div class="col-sm-10">
                    {{form_widget(f.moneyrecived,{'attr':{'class':'form-control'}})}}
                </div>

                <br>
                <div class="col-sm-10">
                    {{form_widget(f.Submit,{'attr':{'class':'btn btn-primary'}})}}
                </div>

                {{ form_end (f) }}
                <!-- End General Form Elements -->

            </div>
        </div>

    </div>
{% endblock %}